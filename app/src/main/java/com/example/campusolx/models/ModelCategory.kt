package com.example.campusolx.models

// A data model class representing a category with a name and an icon
class ModelCategory (
    var category: String, // The name of the category
    var icon: Int          // The resource ID of the category's icon
)
